spring:
  datasource:
    url: jdbc:mysql://${MYSQL_DB:localhost}/iseplive_db?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080
security:
  basic:
    enabled: false
