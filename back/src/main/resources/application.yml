spring:
  datasource:
    url: jdbc:mysql://${MYSQL_DB:localhost}/iseplive_db?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  http:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080
security:
  basic:
    enabled: false

storage:
  url: "storage"
  club:
    url: "/club"
  event:
    url: "/event"
  image:
    url: "/image"
  student:
    url: "/student"
  video:
    url: "/video"
  gazette:
    url: "/gazette"

logging:
  level:
    DEBUG

jwt:
  secret: iseplive-secret
  tokenDuration: 51840000
#  tokenDuration: 604800 # 7 jours
#  tokenDuration: 10
  issuer: IsepLive
