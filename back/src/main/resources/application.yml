spring:
  datasource:
    url: jdbc:mysql://${MYSQL_DB:localhost}/iseplive_db?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  http:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
security:
  basic:
    enabled: false

storage:
  url: "/tmp"
  club:
    url: "/club"